export type WalkthroughItem = {
  step: number;
  label: string;
  title: React.ReactNode;
  description: string;
  cta: string;
  imageUrl: string;
  color: string;
  solidColor: string;
  icon: React.ReactNode;
};
